created: 20260520222000000
modified: 20260520222000000
tags: [[Vocabulary Engine]]
title: Marker Kinds
type: text/vnd.tiddlywiki

The `kind` field of a marker tiddler picks one of six matching modes. The kind determines what source text fires the marker, where the marker can fire (block-position vs inline), and which additional fields are honoured.

!! glyph

Single-fire block marker. The open literal appears at the start of a line; the rest of the line, and possibly the following block, becomes the body.

* Position: block.
* Fires on: open literal followed by whitespace.
* Typical use: paragraph wrappers, line-styled containers.
* Notable fields: `paragraph-marker`, `allow-symbol`, `classes`.
* Worked example: [[vocab/default/DOT]].

!! glyph-level

Multi-fire block marker. Repeating the open literal sets the nesting level (1..maxLevel). The engine emits a `wltc-lN` class corresponding to the level.

* Position: block.
* Fires on: 1..N repetitions of the open literal followed by whitespace.
* Typical use: nested-indent paragraph wrappers.
* Notable fields: `max-level` (default 4), `allow-symbol`.
* Worked example: [[vocab/default/ANGLE]].

!! word

Keyword block marker. The open literal is the keyword itself; whatever follows (single line in inline mode, multiple blocks in block mode) is the body.

* Position: block.
* Fires on: open literal at the start of a line.
* Typical use: section markers, keyword commands (Fountain transitions, markdown headings).
* Notable fields: `mode` (inline vs block), `emit-open`, `end-string`, `paragraph-marker`, `allow-classes`.
* Worked example: [[vocab/fountain/INT]].

!! inline-pair

Bracketed inline marker. Open literal starts the body; matching close literal ends it.

* Position: inline.
* Fires on: open literal at any inline position.
* Typical use: emphasis (italic, bold), code spans, inline brackets.
* Notable fields: `close` (required), `body-raw`, `allow-symbol`, `open-variable` (variable-length delimiter runs — CommonMark-style code spans).
* Worked example: [[vocab/markdown/CODE]].
* Walkthrough: [[Example - Markdown code span]] shows the variable-length backtick-run behaviour with five concrete patterns.

!! linked-pair

Bracketed inline marker with a second bracketed capture immediately after. Source text shape: `open BODY close link-open LINK link-close`. The body becomes either the inline content of the rendered element OR a plain-text attribute on it (when `body-attribute` is set); the LINK capture becomes another attribute (defaulting to `href`). Markdown links `[text](url)` and images `![tooltip](url)` are the canonical use; any DSL can repurpose by picking different bracket literals.

* Position: inline.
* Fires on: full `open BODY close link-open LINK link-close` shape at any inline position. A bare `open` without the trailing brackets falls through to plain text — no fallback needed in parse.
* Typical use: hyperlinks, image references, footnote references, glossary cross-refs, anywhere two bracketed captures sit back-to-back.
* Notable fields: `close` (required), `link-open` / `link-close` (defaults `(` / `)`), `link-attribute` (default `href`), `body-attribute`, `auto-external`, `attr-<name>-from`.
* Worked example: [[vocab/markdown/LINK]] and [[vocab/markdown/IMAGE]].

!! list-item

List bullet marker. Consecutive matches collapse into a single container (ul/ol/dl/blockquote), nesting via indent.

* Position: block.
* Fires on: optional indent + open literal (or `open-pattern` regex) + whitespace, at line start.
* Typical use: markdown list bullets, definition lists, checkbox lists.
* Notable fields: `container`, `item-element`, `indent-unit`, `open-pattern`.
* Worked example: [[vocab/markdown/ITEM-DASH]].

!! Related

* [[Marker Fields]]. Every field on a marker tiddler, grouped by job.
* [[Your first marker tiddler]]. A walkthrough creating one marker.
