{
  "name": "uuid7-server",
  "version": "0.0.1",
  "description": "tiddlywiki edition development helper",
  "main": "index.js",
  "scripts": {
    "start": "node ../scripts/tw.js --core dev . --mcp listen readwrite sse label=uuid7-primary",
    "test": "echo ---- no tests atm ----",

    "server": "npm start",

    "build:github": "node ../scripts/tw.js --core release ../uuid7 --build github",
    "build": "node ../scripts/tw.js --core release ../uuid7 --build index",

    "prestage": "npm run build",
    "stage": "npm run build:github",

    "deploy": "echo # deploy is done with wikilabs.github.io repo!"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/wikilabs/plugins"
  },
  "keywords": [
    "tiddlywiki",
    "tabs",
    "links"
  ],
  "author": "Mario Pietsch",
  "license": "",
  "bugs": {
    "url": "https://github.com/wikilabs/plugins/issues"
  },
  "homepage": "https://wikilabs.github.io/editions/uuid7",
  "dependencies": {
  },
  "devDependencies": {
  }
}
