created: 20260910105947482
modified: 20260918234638045
tags: [[LSP Capabilities]]
title: LSP Find References

Put the cursor on a call and the editor lists every call of that name in the wiki, in its `.tid` files and in tiddlers without a file of their own, through `textDocument/references`. In VS Code that is Shift+F12. Try it in [[LSP Find References - (Example)]].

Each entry opens the file at the name. The cursor may also rest on a definition such as `\procedure name`, and the definition itself is listed whenever the editor asks for it.

!! Every form of a call

A call counts in every form ~TiddlyWiki accepts:

|! Form |! Example |
|Macro call |`<<name>>` |
|`$macrocall` widget |`<$macrocall $name="name"/>` |
|`$transclude` widget |`<$transclude $variable="name"/>` |
|Attribute value |`text=<<name>>` |
|Custom widget |`<$my.widget/>`, defined by `\widget $my.widget()` |
|Filter |`[<name>]`, `[function[name]]`, `[my.fn[]]` |
|Condition |a filter in `<%if%>` or `<%elseif%>` |
|Definition body |a call inside another `\procedure`, `\define`, `\function` or `\widget` |

!! Which text is read

Files are read from disk rather than from the wiki's copy, so a file saved from the editor counts as saved at once. A file open in the editor counts as it is on screen, saved or not.

A shadow tiddler, or a tiddler packed into a `.json` or `.multids` file, has no file of its own, so its calls open in a read-only view. Only the text that runs counts: a tiddler of your own over a plugin's shadow, and a plugin's shadow over the core's. Calls in a `.tid` file's header fields count too, in every field but the title.

!! Parameters and variables

A parameter, or a variable set by a widget, is that name only inside its own scope: its references stay in the definition or widget that binds it, and a closer binding of the same name has references of its own. A name read in a `\function` body is whatever the caller binds, so there it is matched by name everywhere.

!! What cannot be found

Some calls cannot be found. A name assembled at render time (`$variable=<<holder>>`), a `\define` placeholder such as `$param$`, and a backtick filter holding `$(variable)$` or `${ filter }$` are only known once the wiki renders them. Calls are matched by name, so a local `\procedure` that redefines a global one is listed together with it.
